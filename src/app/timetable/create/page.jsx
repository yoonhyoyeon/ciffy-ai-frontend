import TimetableCreateIntro from '@/container/TimetableCreateIntro';

export const metadata = {
    title: '시간표 짜기'
}

export default function TimetableCreatePage() {
    return (
        <TimetableCreateIntro />
    );
}