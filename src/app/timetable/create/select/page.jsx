import TimetableCreateSelect from '@/container/TimetableCreateSelect';

export const metadata = {
    title: '시간표 짜기 - 선택'
}

export default function TimetableCreateSelectPage() {
    return <TimetableCreateSelect />;
}